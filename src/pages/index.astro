---
import type { Locale } from "@/i18n/config"
import { defaultLocale } from "@/i18n/config"

// This page handles the root redirect to the default locale
---

<meta http-equiv="refresh" content={`0;url=/${defaultLocale}`} />
