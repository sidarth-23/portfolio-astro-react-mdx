---
import { Image } from "astro:assets"
import type { CollectionEntry } from "astro:content"
import { Badge } from "@/components/ui/badge"
import { formatShortDate } from "@/lib/content"
import type { Locale } from "@/i18n/config"
import { t } from "@/i18n/ui"

interface Props {
  project: CollectionEntry<"projects">
  locale: Locale
}

const { project, locale } = Astro.props
---

<a
  href={`/${locale}/projects/${project.slug.split("/").slice(1).join("/")}`}
  class="group block h-full"
>
  <article class="flex h-full flex-col overflow-hidden rounded-xl bg-card text-card-foreground ring-1 ring-foreground/10 transition-all hover:shadow-md hover:-translate-y-0.5">
    <div class="overflow-hidden">
      <Image
        src={project.data.coverImage}
        alt={project.data.title}
        width={600}
        height={340}
        class="aspect-video w-full object-cover transition-transform duration-300 group-hover:scale-105"
      />
    </div>
    <div class="flex flex-1 flex-col p-5">
      <div class="flex items-start justify-between gap-2">
        <h3 class="text-lg font-medium leading-snug transition-colors group-hover:text-primary">
          {project.data.title}
        </h3>
        {project.data.featured && (
          <Badge variant="default" className="shrink-0 capitalize">
            {t(locale, "projects.featuredLabel")}
          </Badge>
        )}
      </div>
      <p class="mt-1 text-xs text-muted-foreground">
        {formatShortDate(project.data.date, locale)}
      </p>
      <p class="mt-3 line-clamp-2 text-sm text-muted-foreground/80 flex-1">
        {project.data.summary}
      </p>
      {project.data.tags.length > 0 && (
        <div class="mt-4 flex flex-wrap gap-1.5">
          {project.data.tags.map((tag) => (
            <Badge variant="outline" className="text-[10px] text-muted-foreground/80">#{tag}</Badge>
          ))}
        </div>
      )}
    </div>
  </article>
</a>
